import { generatedBlogSvTypes } from './generated-blog-sv-types';

describe('generatedBlogSvTypes', () => {
  it('should work', () => {
    expect(generatedBlogSvTypes()).toEqual('generated-blog-sv-types');
  });
});
