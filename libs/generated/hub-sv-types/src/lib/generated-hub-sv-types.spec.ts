import { generatedHubSvTypes } from './generated-hub-sv-types';

describe('generatedHubSvTypes', () => {
  it('should work', () => {
    expect(generatedHubSvTypes()).toEqual('generated-hub-sv-types');
  });
});
